/**
 * GlobalSettings interface para configuración global de generación de presentaciones
 */
export interface GlobalSettings {
  systemPrompt: string | null;
  contentOrientation: string | null;
  visualStyle: string | null;
}
